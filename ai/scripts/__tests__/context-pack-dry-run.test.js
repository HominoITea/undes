const test = require('node:test');

test.skip('context-pack dry-run (manual diagnostics)', () => {
  // Manual runner kept in context-pack-dry-run.js:
  //   node ai/scripts/__tests__/context-pack-dry-run.js
  // This skipped test keeps the dry-run check visible in automated test inventory.
});
